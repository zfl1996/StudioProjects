<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <com.ads.abcbank.xx.ui.widget.RecyclerPagerView
        android:id="@+id/rpSlider"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        android:background="#2B363F"
        android:foregroundGravity="center"
        android:contentDescription="@null" />
    <ImageView
        android:id="@+id/imgHolder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:visibility="gone"
        android:src="@mipmap/bg_port"
        android:maxWidth="90000px"
        android:scaleType="fitCenter" />
    <LinearLayout android:id="@+id/llProgress"
        android:layout_width="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content"
        android:gravity="center">
        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <TextView android:id="@+id/txtHint"
            android:textSize="16.0sp"
            android:textColor="#fff"
            android:shadowColor="#2B363F"
            android:shadowRadius="1"
            android:shadowDx="10"
            android:shadowDy="10"
            android:text="初始化播放列表"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="3.0dip"
            android:gravity="center"
            android:background="#00000000"
            />        
    </LinearLayout>
</RelativeLayout>
